[package]
name = "slang"
version = "0.1.0"
edition = "2024"

[features]
default = ["debug-logging"]
debug-logging = []

[dependencies]
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "2.9", features = ["json"] }
